Simplify and refine recently modified code for clarity and consistency without changing behavior.

Follow the canonical playbook in `.cursor/skills/code-simplifier/SKILL.md` exactly.

Scope: recently modified files from the current git diff unless instructed otherwise.

After refinements, verify with `npm run lint` and `npm run typecheck` on affected areas.
