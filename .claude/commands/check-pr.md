Check the current pull request for readiness before human review.

Follow the canonical playbook in `.cursor/skills/check-pr/SKILL.md` exactly.

Also read `AGENTS.md` for branch workflow context and `docs/checklists/pr-checklist.md` for the team checklist.

If no PR number was given, detect the PR for the current branch with `gh pr view`.

Report: status checks, Qodo review status, description completeness, actionable vs informational comments, and recommended next steps (including whether to run `qodo-loop`).
